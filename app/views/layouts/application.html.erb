<!DOCTYPE html>
<html>
  <head>
    <title>Bookers2</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

    <header>
      <nav class="navbar navbar-expand-lg navbar-dark bg-dark"> <!--クラス名は一旦モデルどおり-->
        <div class="container">
          <a class="navbar-brand" href="/">
            <span>Bookers</span>
          </a>

        <div class="collapse navbar-collapse" id="navbarNavDropdown">
          <ul class="navbar-nav ml-auto">

            <li>
              <%= link_to "users/#{user.id}" do%> <!--ログイン時はユーザー詳細ページへ、非ログイン時はルートへ？-->
                <i class="fas fa-home">Home</i>
              <% end %>
            </li>

            <% if user_signed_in? %>
              <li>
                <%= link_to "Users",  %>
              </li>

              <li>
                <%= link_to "Books",  %>
              </li>

              <li>
                <%= link_to "Log out",  %>
              </li>

            <% else %>
              <li>
                <%= link_to "About",  %>
              </li>

              <li>
                <%= link_to "Sign up",  %>
              </li>

              <li>
                <%= link_to "Log in",  %>
              </li>
            <% end %>
    </header>

    <%= yield %>
  </body>
</html>
